"""
Default project styling — pure Worldographer authoritative palette, no overrides.

Used when no project is specified. Maps render in vanilla Worldographer
colours pulled from worldographer_palette.py.
"""

# No palette overrides — use the authoritative Worldographer colours.
PALETTE_OVERRIDES = {}

# No terrain reskins — every terrain uses its standard glyph.
TERRAIN_RESKINS = {}

# No icon reskins — every icon uses its standard inline-SVG drawing.
ICON_RESKINS = {}

# Default backdrop for hex maps (parchment).
HEX_BACKGROUND_COLOR = '#f0e3c2'

# Default backdrop for square / battlemat maps (light blue).
SQUARE_BACKGROUND_COLOR = '#ADD8E6'
